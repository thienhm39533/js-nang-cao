let x;

// Array Literal
const numbers = [27, 30, 33, 70, 57, 102];
const mixed = [12, 'Hello', true, null];

// Array Constructor
const fruits = new Array('tao', 'xoai', 'buoi');

// lay theo soos
x = numbers[4];

//lay theo so +so
x = numbers[1] + numbers[4];

x = `toi thich an ${fruits[0]}`;

// bao nhieu so trong mang
x = numbers.length;

console.log(x);